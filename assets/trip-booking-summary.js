(function(){
'use strict';
var cfgNode=document.querySelector('[data-vb-booking-summary-config]');var hero=document.querySelector('.trip-intel-hero');if(!cfgNode||!hero)return;var cfg={};try{cfg=JSON.parse(cfgNode.textContent||'{}');}catch(e){return;}var api=String(cfg.api||''),tripId=parseInt(cfg.trip_id||0,10);if(!api||!tripId)return;
function esc(v){return String(v===null||v===undefined?'':v).replace(/[&<>'"]/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c];});}
function render(data){var old=document.querySelector('[data-vb-trip-readiness-dock]');if(old)old.remove();var wrap=document.createElement('section');wrap.className='vb-trip-readiness-dock';wrap.setAttribute('data-vb-trip-readiness-dock','');if(!data.ready){wrap.innerHTML='<div class="vb-ready-copy"><span class="vb-ready-score">—</span><span><strong>Booking readiness needs System Upgrade</strong><small>Apply migration 042 to track reservations, confirmations, deadlines, and trip readiness.</small></span></div>';hero.insertAdjacentElement('afterend',wrap);return;}var r=data.readiness||{};if(Number(r.attention||0)>0||Number(r.deadlines_soon||0)>0)wrap.classList.add('is-attention');var flags=[];if(Number(r.outstanding||0)>0)flags.push('<span>'+Number(r.outstanding)+' required open</span>');if(Number(r.ready_to_book||0)>0)flags.push('<span>'+Number(r.ready_to_book)+' ready to book</span>');if(Number(r.deadlines_soon||0)>0)flags.push('<span>'+Number(r.deadlines_soon)+' deadline'+(Number(r.deadlines_soon)===1?'':'s')+' soon</span>');if(Number(r.checkins_soon||0)>0)flags.push('<span>'+Number(r.checkins_soon)+' check-in'+(Number(r.checkins_soon)===1?'':'s')+' soon</span>');if(!flags.length)flags.push('<span>No booking alerts</span>');wrap.innerHTML='<div class="vb-ready-copy"><span class="vb-ready-score">'+Number(r.percent||0)+'%</span><span><strong>'+esc(r.status_label||'Trip readiness')+'</strong><small>'+Number(r.secured||0)+' secured · '+Number(r.bookings_total||0)+' reservations recorded</small></span></div><div class="vb-ready-flags">'+flags.join('')+'<a class="button secondary small" href="'+esc(String(cfg.manage_url||''))+'">Manage bookings →</a></div>';hero.insertAdjacentElement('afterend',wrap);}
fetch(api+'?trip_id='+encodeURIComponent(String(tripId)),{credentials:'same-origin',headers:{'X-Requested-With':'XMLHttpRequest'}}).then(function(r){return r.json().then(function(d){if(!r.ok||!d.ok)throw new Error(d.error||'Request failed');return d;});}).then(function(d){render(d.booking||{});}).catch(function(){/* Trip workspace remains usable if readiness is temporarily unavailable. */});
})();
