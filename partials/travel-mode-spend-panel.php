<?php
if(!isset($userId))return;$vbTravelSpendTripId=(int)($_GET['id']??0);if($vbTravelSpendTripId<1)return;
if(!class_exists('TripSpendExecutionService'))require_once dirname(__DIR__).'/app/Services/TripSpendExecutionService.php';
try{$vbTravelSpendService=new TripSpendExecutionService(db());if(!$vbTravelSpendService->ready())return;$vbTravelSpend=$vbTravelSpendService->snapshot((int)$userId,$vbTravelSpendTripId);if(empty($vbTravelSpend['can_manage']))return;$vbTravelExec=$vbTravelSpend['execution']??[];}catch(Throwable){return;}
$vbTravelCurrency=(string)($vbTravelExec['currency']??'USD');$vbTravelMoney=static fn($v)=>$v===null?'—':($vbTravelCurrency==='USD'?'$':$vbTravelCurrency.' ').number_format((float)$v,2);$vbTravelPace=(string)($vbTravelExec['pace_status']??'unknown');
?>
<link rel="stylesheet" href="<?=e(app_url('assets/trip-spend.css'))?>">
<section class="shell vb-spend-summary" aria-label="Travel Mode budget execution"><div class="vb-spend-summary-card"><div class="eyebrow">Travel Mode · Budget Execution</div><h2><?=in_array($vbTravelPace,['watch','ahead_of_pace','over_budget'],true)?'Spend pace needs attention.':'Live spend is tracking.'?></h2><p>Owner-only recorded spend. Nothing in this panel is written to the offline itinerary copy.</p><div class="vb-spend-summary-metrics"><div><span>Actual</span><strong><?=e($vbTravelMoney($vbTravelExec['actual_spend']??0))?></strong></div><div><span>Remaining</span><strong><?=e($vbTravelMoney($vbTravelExec['remaining_budget']??null))?></strong></div><div><span>Daily allowance</span><strong><?=e($vbTravelMoney($vbTravelExec['remaining_daily_allowance']??null))?></strong></div><div><span>Pace</span><strong><?=e(ucwords(str_replace('_',' ',$vbTravelPace)))?></strong></div></div><p><a href="<?=e(app_url('trip-spend.php?id='.$vbTravelSpendTripId))?>">Review live spend →</a></p></div></section>
