name: Bootstrap Vacation Brain source

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap paused while package chunks upload."
